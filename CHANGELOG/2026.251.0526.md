# 2026.251.0526 — docs(skill): lane contract sync — plan=charly.yml, runner full-evidence + never-leave-VM, cold-reader artifacts-only, one lane per core

## Summary

Syncs the pi-omarchy-eval lane skill to the APPROVED workflow (plan/eval-omarchy-pi-plugin.md §THE WORKFLOW):

- **plan IS the charly.yml**: the config-oracle authors `pr-beds/pr-<N>/charly.yml` directly — the legacy `pr-plans/eval-plan-<N>.json` orchestrator reference removed.
- **eval-runner**: RED-PROBE → EVAL → FULL EVIDENCE EVALUATION (code/diff, check results, .cast screencast, media frames/video into the complete evidence packet, both recording lanes non-empty) → **CLEANUP: NEVER leaves a VM running** (destroy + domstate-gone verified; zero residual domains/locks at handoff).
- **cold-reader**: explicitly ARTIFACTS-ONLY — never from a running VM (the runner has torn everything down).
- **supervisor**: default concurrency = **ONE EVAL LANE PER CPU CORE** (nproc-derived, RAM-capped) — replacing the hardcoded 16-way wording.

## How tested

```
$ grep -rn 'eval-plan\|16-way' skills/ README.md
(no matches)
$ git diff --cached --check
(clean)
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R1 | Stale tokens (JSON plans, 16-way, missing runner-cleanup/artifacts-only) found by the operator guidance sync — corrected here. |
| R2 | N/A. |
| R3 | Single home per concern; the skill points at eval-omarchy's lane contract. |
| R4 | N/A. |
| R4a | N/A. |
| R5 | The JSON-plan wording is removed, not dual-maintained. |
| R6 | feat/ branch; no force-push. |
| R7 | Grep + diff checks pasted; pr-validator gates the merge. |
| R8 | N/A. |
| R9 | N/A. |
| R10 | N/A for skill wording; the lane runs in the current batch. |
| RDD | N/A. |
| ADE | The artifacts-only cold-read strengthens ADE grading. |
| SDD | N/A. |
| B14 | Pasted outputs. |
| B15 | N/A. |
| B17 | N/A. |
| B18 | N/A. |

## Change classification

- Change class: docs/skill wording.
- Verification gate: grep + diff checks; pr-validator.
- Attribution tier: documentation reviewed.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (documentation reviewed)*