# 2026.250.1748 — feat: scaffold pi-omarchy-eval — lane skills + prompts

## Summary

Scaffolds **opencharly/pi-omarchy-eval** — the pi package for the omarchy PR-evaluation lane.

- `.agents/skills/omarchy-eval-lane/SKILL.md` — the pi-agent lane: the four committed agents (config-oracle, eval-runner, supervisor, cold-reader), the TRIAGE → PLAN → RED-PROBE → EVAL → EVIDENCE → COLD-READ → ACCEPT/REPORT state machine, invocation, and the any-system host contract. Points at the binding contract (`eval-omarchy eval/PR-EVAL-LANE.md`) — no procedure restated (R3).
- `.agents/skills/omarchy-eval-golden/SKILL.md` — the golden VM chain runbook (provision/re-provision with the dual-state delete-before-recapture, golden-presence gate, per-PR clone + RED-PROBE, head-freshness preflight); VM mechanics pointed at `/charly-vm:vm` / `/charly-internals:disposable`.
- `prompts/` — `/eval-pr <N>`, `/eval-batch`, `/eval-cold-read <pr>`, all with the publication gate (nothing posts to omacom/omarchy without explicit operator approval).
- The four AGENTS deliberately do NOT live here: their single home is `opencharly/opencharly .pi/agents/` (PR #157) — no duplicate definitions.
- Org-standard merge workflows: `pr-validator.yml` + `tag-on-merge.yml` (same as eval-omarchy), empty `CHANGELOG/`.

## How tested

Pasted evidence from the final committed tree (feat/scaffold @ 607ca0f):

```
$ git ls-files
.github/workflows/pr-validator.yml
.github/workflows/tag-on-merge.yml
README.md
package.json
prompts/eval-batch.md
prompts/eval-cold-read.md
prompts/eval-pr.md
.agents/skills/omarchy-eval-golden/SKILL.md
.agents/skills/omarchy-eval-lane/SKILL.md
$ git status --short
(clean — no output)
$ git diff --cached --check
(clean — no output)
$ git log --oneline -1
607ca0f feat: scaffold pi-omarchy-eval — lane skills + prompts (agents live in the umbrella repo)
```

R10 (post-merge): install the package in the umbrella (`pi install git:github.com/opencharly/pi-omarchy-eval@v<calver>`), then the `pi -p` smoke ladder + the re-validation of an existing eval — recorded in the plan's close-out step (opencharly/opencharly `plan/eval-omarchy-pi-plugin.md` §4).

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R1 (RCA every anomaly) | Scaffold work surfaced one live anomaly during planning (runner frontmatter corruption) — fixed + gated in umbrella #157; nothing anomalous in this repo's scaffold. |
| R2 (blocking sweep) | N/A — no rulebook/API wording changed by this repo; cross-claims all point at their owners. |
| R3 (no duplication) | Skills point at eval-omarchy's lane contract + the umbrella agents; the package is the single home for skills/prompts. |
| R4 (no workarounds) | No sleeps/retries/hand-fixes; the publication gate is a documented contract, not a hack. |
| R4a | N/A — no CI/config bypass; org-standard workflows only. |
| R5 (delete legacy) | New repo — nothing to delete. |
| R6 (git safety) | feat/ branch, no force-push to main, no hook bypass; bootstrap empty commit on main is the org-standard repo-init. |
| R7 (prove the gate) | Pasted above: clean tree, `git ls-files` inventory, `git diff --cached --check` clean; org pr-validator is the merge gate. |
| R8 (hooks) | Standard org workflows only (pr-validator, tag-on-merge) — no bespoke CI. |
| R9 | N/A — no new commands/tools; package contents are docs/prompts. |
| R10 (fresh disposable proof) | Full delivery audit is the plan's close-out step (pi -p smoke ladder + re-validation of an existing eval) — scheduled, not claimed here. |
| RDD | Skill claims mirror running-system facts already proven on eval-omarchy beds (golden chain, RED-PROBE semantics). |
| ADE | The cold-reader rubric is pointed to, not restated. |
| SDD | N/A — no schema/code generated. |
| B14 (no evidence fraud) | Every claim above is a pasted output; the scheduled R10 is explicitly future, not claimed. |
| B15 | N/A — no secrets/auth material in the package. |
| B17 | N/A — no new executable code beyond org workflows. |
| B18 | N/A — no scope-shrinking flags; nothing skipped. |

## Change classification

- Change class: new repo scaffold (docs/skills/prompts + org CI wiring).
- Verification gate: repo-local checks (pasted) + org pr-validator at merge.
- Attribution tier: analysed on a live system (created + pushed live; the delivery audit is the plan's R10 step).

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*