# 2026.251.0738 — ci: per-PR concurrency dedupe on the pr-validator dispatcher

## Summary

Adds the per-PR `concurrency` dedupe to pi-omarchy-eval's `pr-validator` dispatcher (ONE file, `+6`): a re-dispatch or push on the same PR cancels the in-flight run (`cancel-in-progress: true`, group keyed by repo + pr-number), so the head can never carry duplicate same-name `validate / validate` check runs — a duplicate FAILED run kept `mergeable_state: blocked` despite a PASS (measured on #38). Same fix: reusable org workflow + install source (opencharly/.github#84), the umbrella (#162), and eval-omarchy (#39).

The earlier revision bundled the package's docs/skills commits onto this branch by mistake (stale checkout) — the branch is rebuilt from current origin/main with ONLY this CI commit.

## How tested

```
$ git log --oneline origin/main...HEAD
<ci> ci(pr-validator): per-PR concurrency dedupe (a re-dispatch/push cancels the in-flight run — duplicate same-name check runs kept mergeability blocked despite PASS)
$ git diff --stat origin/main...HEAD
 .github/workflows/pr-validator.yml | 6 ++++++
 1 file changed, 6 insertions(+)
$ git diff --cached --check
(clean)
```

## Rulebook compliance

| Rule | How / N/A |
|---|---|
| R1 | #38's blocked-despite-PASS was RCA'd to duplicate same-name check runs — fixed with a per-PR concurrency group. |
| R2 | N/A. |
| R3 | Single source: the reusable org workflow; this is the per-repo copy. |
| R4 | The fix is the remedy. |
| R4a | N/A — gate unchanged. |
| R5 | N/A — additive; the mistaken bundled commits are removed from this branch. |
| R6 | Rebuilt from origin/main; force-with-lease on the feature branch. |
| R7 | Diff pasted; pr-validator gates the merge. |
| R8 | The change IS the CI file. |
| R9 | N/A. |
| R10 | N/A for CI config; exercised live on the next re-dispatch. |
| RDD | Measured #38 outcome. |
| ADE | N/A. |
| SDD | N/A. |
| B14 | The diff is exactly the one file shown. |
| B15 | N/A. |
| B17 | Workflow config. |
| B18 | N/A. |

## Change classification

- Change class: CI workflow copy (concurrency dedupe, one file).
- Verification gate: diff + pr-validator.
- Attribution tier: analysed on a live system.

*Assisted-by: pi openrouter/deepseek/deepseek-v4-flash-0731 (analysed on a live system)*